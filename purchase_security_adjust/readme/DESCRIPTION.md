This module makes the "Purchase Default Secondary UoM" field
(`purchase_secondary_uom_id`) on the product form visible to purchase users
who are not purchase managers.

By default, the field is placed inside the "Vendor Bills" group which is
restricted to purchase managers by the `purchase` module. This module adds
a separate display of the same field for purchase users outside that restriction.
