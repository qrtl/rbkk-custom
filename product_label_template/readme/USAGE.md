Go to *Inventory > Products > Lots/Serial Numbers*, select one or more
lots/serial numbers, then use *Print > Product Label*. The label is rendered from
the label template configured on each lot's product.
